<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Project_Bluetooth.MainPage"
             Shell.NavBarIsVisible="False"
             Title="Bluetooth Scanner">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="20" BackgroundColor="#023502">

            <!-- Заголовок -->
            <Label Text="BluetoothHC06"
                   FontSize="18"
                   HorizontalOptions="Center"
                   FontAttributes="Bold"
                   TextColor="#FFEBCD"
                   SemanticProperties.HeadingLevel="Level1" />

            <!-- Верхние кнопки -->
            <Grid Margin="2" ColumnDefinitions="*,*,*" ColumnSpacing="10">
                <Button Text="Bluetooth"
                        x:Name="Counter5"
                        FontAttributes="Bold"
                        TextColor="#4096ac"
                        BackgroundColor="#0b0b3e"
                        Clicked="On_Off_Bluetooth"
                        Grid.Column="0" />

                <Button Text="Scan_devices"
                        FontAttributes="Bold"
                        TextColor="#4096ac"
                        BackgroundColor="#000000"
                        Clicked="OnStartScanClicked"
                        Grid.Column="1" />

                <Button Text="Clear_devices"
                        FontAttributes="Bold"
                        TextColor="#4096ac"
                        BackgroundColor="#000000"
                        Clicked="OnStopScanClicked"
                        Grid.Column="2" />
            </Grid>

            <!-- Индикатор поиска -->
            <ActivityIndicator x:Name="activityIndicator"
                               IsRunning="False"
                               IsVisible="False"
                               Color="#FFFFFF"
                               VerticalOptions="Center"
                               HorizontalOptions="Center"
                               HeightRequest="25"
                               WidthRequest="25"/>

            <!-- Список устройств -->
            <ScrollView Orientation="Vertical" HeightRequest="50">
                <FlexLayout BindableLayout.ItemsSource="{Binding DiscoveredDevices}"
                            Direction="Row"
                            Wrap="Wrap"
                            JustifyContent="Start"
                            AlignItems="Start"
                            Margin="0,1,0,0">

                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <Border Stroke="MidnightBlue"
                                    StrokeShape="RoundRectangle 10"
                                    Margin="1"
                                    Padding="4"
                                    BackgroundColor="Khaki"
                                    HorizontalOptions="Start">

                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnDeviceTapped"/>
                                </Border.GestureRecognizers>

                                <VerticalStackLayout Spacing="2"
                                                     HorizontalOptions="Center">
                                    <Label Text="{Binding Name}"
                                           FontSize="14"
                                           TextColor="Blue"
                                           HorizontalOptions="Center" />
                                    <Label Text="{Binding Address}"
                                           FontSize="9"
                                           TextColor="DarkBlue"
                                           HorizontalOptions="Center" />
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </FlexLayout>
            </ScrollView>


            <HorizontalStackLayout HorizontalOptions="Center" Spacing="20">

                                <Button Text="debug"
                x:Name="DebugLabelButton"
                Clicked="ShowPage1Clicked"
                BackgroundColor="Transparent"
                TextColor="White"
                FontSize="20"
                Padding="4" />

                                <Button Text="❮"
                x:Name="Page1Indicator"
                Clicked="ShowPage1Clicked"
                BackgroundColor="Transparent"
                TextColor="White"
                FontSize="26"
                Padding="8" />

                                <Button Text="❯"
                x:Name="Page2Indicator"
                Clicked="ShowPage2Clicked"
                BackgroundColor="Transparent"
                TextColor="Gray"
                FontSize="26"
                Padding="8" />

                            <Button Text="user"
                x:Name="UserLabelButton"
                Clicked="ShowPage2Clicked"
                BackgroundColor="Transparent"
                TextColor="Gray"
                FontSize="20"
                Padding="4" />  

            </HorizontalStackLayout>

            <Grid>

                <Grid.GestureRecognizers>
                    <SwipeGestureRecognizer Direction="Left" Swiped="OnSwipedLeft"/>
                    <SwipeGestureRecognizer Direction="Right" Swiped="OnSwipedRight"/>
                </Grid.GestureRecognizers>

                <!-- Страница 1 -->
                <VerticalStackLayout x:Name="Page1Block"
                                     Spacing="10"
                                     IsVisible="True">

                    <Border Stroke="#0B0101"
                            StrokeThickness="2"
                            StrokeShape="RoundRectangle 10"
                            BackgroundColor="#b3d6bf"
                            Padding="10"
                            Margin="5"
                            HeightRequest="180">

                        <ScrollView x:Name="scrollView" VerticalOptions="Fill">
                            <Label x:Name="label4"
                                   Text="Empty text to receive data"
                                   FontAttributes="Bold"
                                   FontSize="14"
                                   TextColor="DarkBlue"
                                   LineBreakMode="WordWrap"
                                   VerticalTextAlignment="Start"
                                   HorizontalTextAlignment="Start" />
                        </ScrollView>

                    </Border>

                    <Grid Margin="5" ColumnDefinitions="*,Auto,*" ColumnSpacing="10">
                        <Button Margin="5"
                                Text="Receiver"
                                x:Name="Counter4"
                                FontAttributes="Bold"
                                TextColor="#33cccc"
                                BackgroundColor="#18182C"
                                Clicked="DataReciever"
                                Grid.Column="0" />

                        <Button Margin="5"
                                Text="Clear data"
                                FontAttributes="Bold"
                                TextColor="#01010d"
                                BackgroundColor="#70CBCB"
                                Clicked="OnClearDevices"
                                Grid.Column="1" />

                        <Button Margin="5"
                                Text="Transmitter"
                                x:Name="Counter1"
                                FontAttributes="Bold"
                                TextColor="#33cccc"
                                BackgroundColor="#18182C"
                                Clicked="TransmitterData"
                                Grid.Column="2" />
                    </Grid>

                    <Border Stroke="Gray"
                            StrokeThickness="1"
                            Padding="10"
                            Margin="5">
                        <Entry x:Name="entry1"
                               Placeholder="Enter text to transfer data"
                               PlaceholderColor="BlanchedAlmond"
                               FontSize="18"
                               TextColor="Black"
                               HorizontalOptions="Fill"
                               VerticalOptions="Center" />
                    </Border>


                                <Button Text="Privacy Policy"
                                        HorizontalOptions="Center"
                                        Margin="10,20,10,10"
                                        Clicked="OnPrivacyPolicyClicked"
                                        FontAttributes="Bold"
                                        TextColor="#741818"
                                        BackgroundColor="#000000"/> 






                </VerticalStackLayout>

                <!-- Страница 2 -->
                <VerticalStackLayout x:Name="Page2Block"
                                     Spacing="12"
                                     IsVisible="False">

                    <!--<Label Text="Grid 128"
                           FontSize="20"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           TextColor="White" />-->

                    <GraphicsView x:Name="grid128View"
                  HeightRequest="420"
                  WidthRequest="360"
                  HorizontalOptions="Center"
                  BackgroundColor="Black" />
                </VerticalStackLayout>

            </Grid>

            

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>